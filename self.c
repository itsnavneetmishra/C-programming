#include<stdio.h>
int add(int,int);
void main()
{
    int calc(int x,int y)
    {
        int add(int x, int y)
        {
            int c=x+y;
            return c;
    
        }

        int subt(int x, int y)
        {
            int c=x-y;
            return c;
        }

        int multiplication(int x, int y)
        {
            int c=x*y;
            return c;
        }

        int division(int x,int y)
        {
            int c=x/y;
            return c;
        }
    }
}